class Tools {
  constructor(icon, name) {
    //set an icon and a name for the object
    this.icon = icon;
    this.name = name;
    this.isDrawing = false;
  }

  displayConfigOptions() {
    return [];
  }

  draw() {}
}
